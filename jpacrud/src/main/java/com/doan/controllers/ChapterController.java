package com.doan.controllers;

import org.springframework.stereotype.Controller;

@Controller
public class ChapterController {

	/*
	 * @Autowired ChapterService chapterServicel;
	 * 
	 * @RequestMapping(value = "/getdschap", method = RequestMethod.GET)
	 * 
	 * @ResponseBody public String getds(){ List<Chapter> listCustomer =
	 * chapterServicel.getAllChapByIdcomic(); Gson json = new Gson(); String
	 * listjson = json.toJson(listCustomer); return listjson; }
	 */
	}
